# Database Configuration - MUST be set via environment variables in production
DATABASE_URL=postgresql://postgres:sartaevaidar2008@localhost:5000/rsl_kazakhstan
PORT=4000

# JWT Configuration - CRITICAL: Set a strong secret in production (min 32 chars)
JWT_SECRET=your_super_secure_jwt_secret_key_at_least_32_chars_long_replace_in_production

# Rate Limiting Configuration
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

